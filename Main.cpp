#include <chrono>
#include <iostream> 

#include "Cola.h"
#include "ColaPrioritaria.h"
#include "GestorClientes.h"
#include "Graph.h"
#include "HashMap.h"
#include "ListaCuentas.h"
#include "Pila.h"
#include "SistemaBancario.h"
#include "Transaccion.h"

using namespace std;
 
int main()
{
	/* 
     SistemaBancario sistema;
	 sistema.menuPrincipal(); 
		*/

	 
	/*Grafo g;
	g.crearSucursales();
	g.establecerAristas();
	g.prim();
    g.kruskal();
	g.BFS(0);
	
	int visited[5] = {0,0,0,0,0};

	cout << "\nDepth first search\n";
	g.DFS(0, visited);
	*/




	// entregable 5 
	/*HashMap hash(10);
	hash.insert("valor1", "password1");
	hash.insert("valor2", "password2");
	hash.insert("valor3", "password3");
	hash.insert("valor4", "password4");*/

	//prueba del hashmap 
	/*if (hash.search("valor1", "password1")) {
		cout << "se encontro el valor\n";
	}
	else {
		cout<<"No se encontro el valor\n";
	}
	if (hash.search("valor1", "dfsfs")) {
		cout << "se encontro";
	}
	else {
		cout << "no se encontro porque no se ingreso la contrasena correcta\n";
	}*/


	return 0;
}